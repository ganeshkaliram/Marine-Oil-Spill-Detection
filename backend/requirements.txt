# Python 3.14 compatible pins (verified working in this environment).
# Scientific/ML pins below (torch, rasterio, geopandas) are placeholders — they
# may lag Python 3.14 wheel availability. Install them in a Python 3.11/3.12
# environment for training, or upgrade pins as wheels ship.

fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.13
pydantic-settings>=2.15

# Data / science
numpy>=2.2
pandas>=2.2
geopandas>=1.0
shapely>=2.0
pyproj>=3.7
rasterio>=1.4
xarray>=2025.1

# ML / CV (recommend Python 3.11/3.12 for training)
torch>=2.5
torchvision>=0.20
scikit-learn>=1.6
segmentation-models-pytorch>=0.4

# GIS / web stack
requests>=2.32
python-dotenv>=1.0

# Testing
pytest>=8.3
httpx>=0.28

# Notebooks
jupyter>=1.1
ipykernel>=6.29
